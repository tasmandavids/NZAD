# Claude Code local settings (may contain tokens)
.claude/settings.local.json

# dependencies
node_modules/

# next.js
/.next
/out
next-env.d.ts
*.tsbuildinfo

# env & secrets
.env*.local
.env.vercel.*

# vercel
.vercel

# supabase cli (local link state — not for git)
supabase/.temp/
supabase/.branches/

# os / editors
.DS_Store
*.log
.vscode/
.idea/

# legacy wix starter (unused)
.wix/
jsconfig.json

# local tooling artifacts (never commit — large binaries / scratch deps)
.tmp_pydeps/
supabase_*.tar.gz
*.tar.gz
