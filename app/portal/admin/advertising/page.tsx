import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function AdvertisingPage() {
  return (
    <AdminPlaceholder
      title="Advertising"
      description="Promote your studio with campaigns and ad tools. This section is on the roadmap — we will build it out together."
    />
  );
}
